{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "bash -c 'cd app && npm install && npx prisma generate --engine-type binary && npm run build'"
    },
    "deploy": {
        "startCommand": "bash -c 'cd app && npx prisma migrate deploy && npm run start'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}