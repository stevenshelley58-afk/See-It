SHOPIFY_API_KEY=
# Used in app/shopify.server.js

SHOPIFY_API_SECRET=
# Used in app/shopify.server.js

SCOPES=write_products,read_products
# Used in app/shopify.server.js

HOST=
# Used in app/shopify.server.js (often mapped to SHOPIFY_APP_URL)

SHOPIFY_APP_URL=
# Used in app/shopify.server.js and app/routes/api.billing.jsx

DATABASE_URL="postgresql://user:password@localhost:5432/seeit_dev"
# Used in app/db.server.js and prisma/schema.prisma

SHOP_CUSTOM_DOMAIN=
# Optional: Used in app/shopify.server.js

IMAGE_SERVICE_BASE_URL=
# Used in app-proxy routes for image service calls

IMAGE_SERVICE_TOKEN=
# Shared secret for Image Service auth

# Required for product preparation (Gemini AI)
GEMINI_API_KEY=your-gemini-api-key

# Required for image storage (Google Cloud Storage)
GOOGLE_CREDENTIALS_JSON=base64-encoded-service-account-json
GCS_BUCKET=see-it-room

# Optional configuration
DISABLE_PREPARE_PROCESSOR=false
SHOPIFY_BILLING_TEST_MODE=true
