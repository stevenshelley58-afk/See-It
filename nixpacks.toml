[phases.setup]
nixPkgs = ["openssl", "openssl_1_1", "nodejs_20"]

[phases.install]
cmds = ["cd app && npm ci"]

[phases.build]
cmds = ["cd app && npx prisma generate && npm run build"]

[start]
cmd = "cd app && npm run start"
