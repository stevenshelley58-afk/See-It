# External API Configuration
RAILWAY_API_URL=https://see-it-production.up.railway.app
MONITOR_API_TOKEN=
MONITOR_REVEAL_TOKEN=

# Database (same as main app - required for internal prompt-control APIs)
# Railway integration provides DATABASE_PUBLIC_URL; code accepts either
DATABASE_URL=postgresql://...
# DATABASE_PUBLIC_URL=postgresql://...

# Authentication Configuration
# MONITOR_ALLOW_IMPLICIT_DASHBOARD_AUTH=true
# Set to "true" to enable implicit authentication for dashboard pages in production.
# In development (NODE_ENV !== "production"), implicit auth is enabled by default.
# When enabled, browser requests to protected /api/* routes (everything except /api/health and /api/auth)
# will automatically receive admin access if MONITOR_API_TOKEN is set, without requiring Authorization headers.
