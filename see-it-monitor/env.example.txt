# See It Session Monitor - Environment Variables

# Google Cloud Storage credentials
# Option 1: Individual credentials
GCS_PROJECT_ID=your-project-id
GCS_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Option 2: JSON credentials (base64 encoded or raw)
# GOOGLE_CREDENTIALS_JSON={"type":"service_account",...}

# Session bucket name
GCS_SESSION_BUCKET=see-it-sessions

# Dashboard password (for basic auth if implemented)
DASHBOARD_PASSWORD=your-secret-password
