# Database (required)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Admin endpoints (required)
MIGRATE_SECRET=generate_a_long_random_secret
# Optional: can be set separately; falls back to MIGRATE_SECRET
BACKFILL_SECRET=generate_a_long_random_secret

# Google Cloud Storage Configuration
GOOGLE_CREDENTIALS_JSON=base64_encoded_or_raw_json_string
GCS_SESSION_BUCKET=see-it-sessions

# Note: GOOGLE_CREDENTIALS_JSON can be:
# 1. Base64-encoded JSON string
# 2. Raw JSON string (must be on a single line or properly escaped)
# 3. The service account JSON object as a string
